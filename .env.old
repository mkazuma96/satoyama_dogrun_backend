# データベース設定（優先度: 以下の MySQL 環境変数が揃っていれば MySQL を使用。未設定なら DATABASE_URL を参照）
DB_USER=
DB_PASSWORD=
DB_HOST=
DB_PORT=3306
DB_NAME=
# SSL CA 証明書のパス（backend/db_control 配下に置く場合の例）
SSL_CA_PATH=certs/DigiCertGlobalRootCA.crt.pem

# Fallback 用（未設定時は SQLite）
DATABASE_URL=sqlite:///./satoyama_dogrun.db

# JWT設定
SECRET_KEY=your-secret-key-here-change-this-in-production
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30

# サーバー設定
HOST=0.0.0.0
PORT=8000
DEBUG=false

# CORS設定
ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# ファイルアップロード設定
MAX_FILE_SIZE=10485760
ALLOWED_FILE_TYPES=image/jpeg,image/png,image/gif

# パスワード設定
MIN_PASSWORD_LENGTH=8

# ログ設定
LOG_LEVEL=INFO

# アプリケーション設定
APP_NAME=里山ドッグラン API
APP_VERSION=1.0.0 

# マイグレーション中の安全装置: true の場合のみアプリ起動時にテーブル自動作成
AUTO_CREATE_TABLES=false